generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "sqlite"
    url      = env("DATABASE_URL")
}

model Like {
  id        String   @id @default(cuid())
  slug      String   @unique
  count     Int      @default(0)
  updatedAt DateTime @updatedAt
}